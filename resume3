<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sunnie Jae Fakebook</title>
  <style>
    :root {
      --facebook-blue: #3b5998;
      --light-blue: #dfe3ee;
      --white: #ffffff;
      --black: #000000;
      --gray: #90949c;
      --pale-pink: #ffd6e7;
      --light-gray: #e6e6e6;
    }

    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background-color: var(--light-blue);
    }

    /* Header */
    .facebook-header {
      background-color: var(--facebook-blue);
      color: var(--white);
      padding: 10px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .facebook-logo {
      font-size: 24px;
      font-weight: bold;
    }
    .facebook-search input {
      padding: 5px;
      border: none;
      border-radius: 4px;
    }

    /* Layout */
    .main-container {
      display: flex;
      margin: 20px;
    }
    .left-sidebar, .right-sidebar {
      width: 200px;
      background: var(--white);
      padding: 15px;
      border-radius: 8px;
      height: fit-content;
    }
    .main-content {
      flex: 1;
      margin: 0 20px;
    }

    /* Profile Header */
    .profile-header {
      background: var(--white);
      padding: 20px;
      border-radius: 8px;
      text-align: center;
      margin-bottom: 20px;
    }
    .profile-header h1 {
      margin: 0;
    }
    .profile-tabs {
      display: flex;
      justify-content: center;
      margin-top: 10px;
    }
    .profile-tabs div {
      margin: 0 10px;
      cursor: pointer;
      color: var(--facebook-blue);
    }

    /* Info Sections */
    .info-content {
      background: var(--white);
      padding: 20px;
      border-radius: 8px;
    }
    .info-section {
      margin-bottom: 20px;
    }
    .section-header {
      font-size: 20px;
      font-weight: bold;
      margin-bottom: 10px;
      border-bottom: 2px solid var(--facebook-blue);
      padding-bottom: 5px;
    }
    .section-content h4 {
      margin-top: 15px;
    }

    /* Chat styles */
    #inboxBtn {
      position: fixed;
      bottom: 20px;
      right: 20px;
      background: var(--facebook-blue);
      color: var(--white);
      border: none;
      border-radius: 50px;
      padding: 12px 20px;
      font-size: 16px;
      cursor: pointer;
    }
    #notification {
      background: red;
      color: white;
      border-radius: 50%;
      padding: 3px 8px;
      font-size: 12px;
      margin-left: 6px;
      animation: flash 1s infinite;
      display: none;
    }
    @keyframes flash {
      0% { opacity: 1; }
      50% { opacity: 0; }
      100% { opacity: 1; }
    }

    #chatWindow {
      position: fixed;
      bottom: 70px;
      right: 20px;
      width: 300px;
      background: var(--white);
      border: 1px solid var(--gray);
      border-radius: 8px;
      display: none;
      flex-direction: column;
      box-shadow: 0 2px 8px rgba(0,0,0,0.2);
    }
    #chatHeader {
      background: var(--facebook-blue);
      color: white;
      padding: 8px;
      font-weight: bold;
    }
    #chatMessages {
      height: 200px;
      overflow-y: auto;
      padding: 10px;
    }
    #chatInput {
      border: none;
      border-top: 1px solid var(--light-gray);
      padding: 10px;
      width: calc(100% - 20px);
    }
  </style>
</head>
<body>

  <!-- Header -->
  <div class="facebook-header">
    <div class="facebook-logo">Fakebook</div>
    <div class="facebook-search">
      <input type="text" placeholder="Search">
    </div>
  </div>

  <!-- Layout -->
  <div class="main-container">
    <!-- Left Sidebar -->
    <div class="left-sidebar">
      <h3>Navigation</h3>
      <ul>
        <li>Home</li>
        <li>Profile</li>
        <li>Friends</li>
      </ul>
    </div>

    <!-- Main Content -->
    <div class="main-content">
      <div class="profile-header">
        <h1>Sunnie Jae</h1>
        <div class="profile-tabs">
          <div>Timeline</div>
          <div>About</div>
          <div>Friends</div>
        </div>
      </div>

      <div class="info-content">
        <!-- Professional Resume -->
        <div class="info-section">
          <div class="section-header">Professional Resume</div>
          <div class="section-content">
            <h3>Recent Work History</h3>
            <div class="work-entry">
              <div class="work-title">Senior Supervisor, Sales Operations</div>
              <div class="work-dates">Oct 2022 – Present</div>
              <p>Managed up to 15 agents, led competitions, built SharePoint sites, and integrated design into workflows.</p>
            </div>
          </div>
        </div>

        <!-- Unprofessional Resume -->
        <div class="info-section">
          <div class="section-header">Unprofessional Resume</div>
          <div class="section-content">
            <p>When you get Sunnie Jae, you get options. I'm a creative by nature; I take every chance to turn an idea into an experience.</p>
            <h4>Writing</h4>
            <p>I've been writing longer than anything. Language is my first love. (Sorry, Ken)</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Right Sidebar -->
    <div class="right-sidebar">
      <h3>Sponsored</h3>
      <p>Ad content here.</p>
    </div>
  </div>

  <!-- Floating Inbox Button -->
  <button id="inboxBtn">Inbox <span id="notification">!</span></button>

  <!-- Chat Window -->
  <div id="chatWindow">
    <div id="chatHeader">Chat with Sunnie</div>
    <div id="chatMessages"></div>
    <input type="text" id="chatInput" placeholder="Type a message...">
  </div>

  <script>
    const inboxBtn = document.getElementById("inboxBtn");
    const chatWindow = document.getElementById("chatWindow");
    const notification = document.getElementById("notification");
    const chatMessages = document.getElementById("chatMessages");
    const chatInput = document.getElementById("chatInput");

    // Toggle chat window
    inboxBtn.addEventListener("click", () => {
      chatWindow.style.display = chatWindow.style.display === "flex" ? "none" : "flex";
      notification.style.display = "none"; // clear alert when opened
    });

    // Fake "new message" notification after delay
    setTimeout(() => {
      notification.style.display = "inline-block";
      chatMessages.innerHTML += "<div><b>Sunnie:</b> Hey! Thanks for checking out my profile 💜</div>";
    }, 3000);

    // Send message
    chatInput.addEventListener("keypress", e => {
      if (e.key === "Enter" && chatInput.value.trim() !== "") {
        chatMessages.innerHTML += "<div><b>You:</b> " + chatInput.value + "</div>";
        chatInput.value = "";
        chatMessages.scrollTop = chatMessages.scrollHeight;
      }
    });
  </script>

</body>
</html>
